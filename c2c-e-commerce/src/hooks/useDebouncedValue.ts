"use client";

/**
 * C2C-AI-8 — value debouncing.
 *
 * SPEC PHASE SKELETON.
 */
export function useDebouncedValue<T>(_value: T, _delayMs: number): T {
  throw new Error("not implemented — C2C-AI-8 is in its spec phase");
}
