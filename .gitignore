node_modules
.env
*.env.local
.next/

.env
.env.example

# .env.example carries placeholders only and is meant to be committed.
!.env.example

# Process documents, per the backlog's commit-hygiene rules: implementation plans,
# spec write-ups and session notes are working notes, not deliverables, and stay out
# of the repository. The files remain on disk for whoever is working the story.
docs/backlog/plans/
